APP_ENV=development
APP_URL=http://localhost:8080
# Force HTTPS redirects + HSTS header. Enable in production.
APP_FORCE_HTTPS=false

DB_HOST=db
DB_PORT=5432
DB_NAME=furever
DB_USER=furever
DB_PASSWORD=changeme

SESSION_LIFETIME=7200
UPLOAD_MAX_BYTES=2097152
